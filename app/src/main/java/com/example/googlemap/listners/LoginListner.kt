package com.example.googlemap.listners

import androidx.lifecycle.LiveData
import androidx.lifecycle.MutableLiveData
import com.example.googlemap.loginResponse.LoginResponse

interface LoginListner {
    fun start()
}